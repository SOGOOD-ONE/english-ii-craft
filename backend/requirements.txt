# English-II-Craft Backend (Django 5.1 + DRF 3.15)
# Python >= 3.10
Django>=5.1,<5.2
djangorestframework>=3.15.2,<3.16
drf-spectacular>=0.27.2          # 自动生成 Swagger/OpenAPI 文档: GET /api/schema/docs
djangorestframework-simplejwt>=5.3.1 # JWT 鉴权: /api/v1/auth/token + /token/refresh
django-cors-headers>=4.4.0       # 前端 5173 → 后端 8000 跨域
# ---------- 业务依赖 ----------
# 官方 FSRS v5 Python 实现(py-fsrs):  https://pypi.org/project/fsrs/   (包名fsrs,版本 5.x)
fsrs>=5,<6
# 字符级 Diff(翻译尝试对比)
diff-match-patch>=20230430
# HTTP 调用 AI Provider(智谱/DeepSeek/Moonshot 共用 OpenAI 兼容协议)
httpx>=0.27.2
# 中文 / 英文词还原(Porter/Lancaster 词干 + 中文停用词,用于 hover 查词 lemma)
nltk>=3.8.1
